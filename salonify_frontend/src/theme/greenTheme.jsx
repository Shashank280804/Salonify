import { createTheme } from "@mui/material/styles"; 

const greenTheme = createTheme({
    palette:{
        mode: "light",
        primary:{
            main: "#019031"
        },
        secondary:{
            main: "#EAF0F1"
        }
    }
})

export default greenTheme;